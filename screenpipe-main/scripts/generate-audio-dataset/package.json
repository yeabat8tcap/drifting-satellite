{
  "name": "generate-audio-dataset",
  "private": true,
  "version": "1.0.0",
  "description": "Generate audio benchmark dataset using ElevenLabs TTS",
  "scripts": {
    "generate": "bun run index.ts",
    "generate:small": "bun run index.ts --small"
  },
  "dependencies": {
    "elevenlabs": "^1.0.0",
    "wav": "^1.0.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.0.0"
  }
}
